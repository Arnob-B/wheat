import { Card, CardTitle, CardFooter, CardHeader, CardContent, CardDescription } from "@/components/ui/card"
import { Drawer, DrawerClose, DrawerTitle, DrawerHeader, DrawerFooter, DrawerPortal, DrawerContent, DrawerOverlay, DrawerTrigger, DrawerDescription } from "@/components/ui/drawer"
export default function Reviews() {
  return (
    <div>
      <Drawer>
        <DrawerTrigger>
          open
        </DrawerTrigger>
      </Drawer>
    </div>
  )

}
